# Test generated by RoostGPT for test testArtillery using AI Type Open AI and AI Model gpt-4

# RoostTestHash=977dd819cb

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "nobelPrizes_get_others.csv"
    fields:
      - "offset"
      - "limit"
      - "sort"
      - "nobelPrizeYear"
      - "yearTo"
      - "nobelPrizeCategory"
      - "format"
      - "csvLang"
    skipHeader: true

scenarios:
  - name: "Testing nobelPrizes API for error cases"
    flow:
      - log: "Testing with invalid offset"
        get:
          url: "/nobelPrizes?offset=-1"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 400
            - hasProperty: "code"
            - hasProperty: "message"
      - think: 5
      - log: "Testing with invalid limit"
        get:
          url: "/nobelPrizes?limit=-1"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 400
            - hasProperty: "code"
            - hasProperty: "message"
      - think: 5
      - log: "Testing with invalid nobelPrizeYear"
        get:
          url: "/nobelPrizes?nobelPrizeYear=1800"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 400
            - hasProperty: "code"
            - hasProperty: "message"
      - think: 5
      - log: "Testing with invalid yearTo without providing nobelPrizeYear"
        get:
          url: "/nobelPrizes?yearTo=2000"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 422
            - hasProperty: "code"
            - hasProperty: "message"
      - think: 5
      - log: "Testing with invalid URL"
        get:
          url: "/invalidURL"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 404
            - hasProperty: "code"
            - hasProperty: "message"
      - think: 5
      - log: "Testing with invalid token"
        get:
          url: "/nobelPrizes"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
