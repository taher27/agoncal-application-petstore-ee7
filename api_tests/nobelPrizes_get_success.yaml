# Test generated by RoostGPT for test testArtillery using AI Type Open AI and AI Model gpt-4

# Test generated for /nobelPrizes_get for http method type GET in artillery framework
# RoostTestHash=977dd819cb

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
scenarios:
  - name: "Test Nobel Prizes API"
    flow:
      - get:
          url: "/nobelPrizes"
          qs:
            offset: 1
            limit: 1
            sort: "asc"
            nobelPrizeYear: 2000
            yearTo: 2010
            nobelPrizeCategory: "eco"
            format: "json"
            csvLang: "en"
          expect:
            - statusCode: 200
            - hasProperty: "nobelPrizes"
            - hasProperty: "meta"
            - hasProperty: "links"
          log: "Testing Nobel Prizes API with valid query parameters"
        think: 1
      - get:
          url: "/nobelPrizes"
          qs:
            offset: 5
            limit: 5
            sort: "desc"
            nobelPrizeYear: 1990
          expect:
            - statusCode: 200
            - hasProperty: "nobelPrizes"
            - hasProperty: "meta"
            - hasProperty: "links"
          log: "Testing Nobel Prizes API with different valid query parameters"
        think: 1
      - get:
          url: "/nobelPrizes"
          expect:
            - statusCode: 200
            - hasProperty: "nobelPrizes"
            - hasProperty: "meta"
            - hasProperty: "links"
          log: "Testing Nobel Prizes API without any query parameters"
        think: 1
