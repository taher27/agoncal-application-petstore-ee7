# Test generated by RoostGPT for test testArtillery using AI Type Open AI and AI Model gpt-4

# Test generated for /laureate/{laureateID}_get for http method type GET in artillery framework
# RoostTestHash=528d0d51bc

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "laureate_laureateID_get_success.csv"
    fields:
      - "laureateID"
    skipHeader: true
scenarios:
  - name: "Test API endpoint /laureate/{laureateID}"
    flow:
      - get:
          url: "/2.1/laureate/{{ laureateID }}"
          beforeRequest:
            - "log": "Sending request to /laureate/{{ laureateID }}"
          expect:
            - statusCode: 200
            - hasProperty: "code"
            - hasProperty: "message"
          afterResponse: 
            - "log": "Received response from /laureate/{{ laureateID }}"
          think: 1
      - get:
          url: "/2.1/laureate/{{ laureateID }}"
          beforeRequest:
            - "log": "Sending request to /laureate/{{ laureateID }} with invalid ID"
          expect:
            - statusCode: 400
            - hasProperty: "code"
            - hasProperty: "message"
          afterResponse: 
            - "log": "Received response from /laureate/{{ laureateID }} with invalid ID"
          think: 1
      - get:
          url: "/2.1/laureate/{{ laureateID }}"
          beforeRequest:
            - "log": "Sending request to /laureate/{{ laureateID }} with non-existent ID"
          expect:
            - statusCode: 404
            - hasProperty: "code"
            - hasProperty: "message"
          afterResponse: 
            - "log": "Received response from /laureate/{{ laureateID }} with non-existent ID"
          think: 1
