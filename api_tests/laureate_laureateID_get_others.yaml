# Test generated by RoostGPT for test testArtillery using AI Type Open AI and AI Model gpt-4

# RoostTestHash=528d0d51bc

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "laureate_laureateID_get_others.csv"
    fields:
      - "laureateID"
    skipHeader: true

scenarios:
  - name: "API endpoint failure and edge case testing"
    flow:
      - log: "Testing with invalid laureateID"
        get:
          url: "/2.1/laureate/{{ laureateID }}"
          expect:
            - statusCode: 400
            - hasProperty: "code"
            - hasProperty: "message"
        think: 1
      - log: "Testing with non-existing laureateID"
        get:
          url: "/2.1/laureate/{{ laureateID }}"
          expect:
            - statusCode: 404
            - hasProperty: "code"
            - hasProperty: "message"
        think: 1
      - log: "Testing with invalid URL"
        get:
          url: "/2.1/invalid/{{ laureateID }}"
          expect:
            - statusCode: 422
            - hasProperty: "code"
            - hasProperty: "message"
        think: 1
